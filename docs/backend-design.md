# Backend Design

Responsibilities

- Load Pokémon data
- Apply filters
- Generate random Pokémon
- Return results to the frontend

API Endpoints (planned)

GET /

Returns a message confirming the backend is running.

GET /generate

Returns three randomly generated Pokémon.

Future Endpoints

POST /generate
GET /pokemon
GET /health